import 'package:flutter/material.dart';

class GtaCountdownPage extends StatelessWidget {
  const GtaCountdownPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}